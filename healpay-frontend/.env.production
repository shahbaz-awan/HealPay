# Production — Vercel reads this automatically when NODE_ENV=production
# This URL must point to your Render backend service
# Update this AFTER you deploy the backend to Render and get the URL
VITE_API_BASE_URL=https://healpay-backend.onrender.com/api
